simulation:
  type: aws

  limits:
    max_cpu: 10
    max_ram_gb: 5

  aws_data_path: simulation/aws/aws.csv

  metrics_poll_interval: 0.1

workload:
  name: 'Factorize'
  image: 063fce619bc1

algorithm:
  episodes: 1

policy:
  max_cost: 200
